import React from 'react'

export const Buttonthree = ({setModal}) => {
  return (
    <button className="primaryBtn" data-aos='fade-up-right'
    onClick={()=>setModal(true)}>UNIDAD 1</button>
  )
}
